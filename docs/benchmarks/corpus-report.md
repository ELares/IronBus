# IronBus competitor benchmark corpus

Generated by `assemble-corpus` from `corpus_bench.py` rows. Same device (RPi4 armv7, loopback), matched durability per tier. Throughput is the comparable metric (msgs/sec); the durability-label lint in `ComparisonReport::build` rejects any mismatched (marketing) pair at assemble time.

## Publish throughput, matched durability (head-to-head)

### `sync-per-message` -- durable, power-loss-safe (one fdatasync per ack)

| payload | IronBus | NATS JetStream | Redis Streams |
| --- | --- | --- | --- |
| 256 B | 211 | 179 | 181 |
| 1024 B | 192 | 153 | 178 |
| 4096 B | 182 | 132 | 150 |

### `page-cache-async` -- NOT power-loss-safe (ack from page cache / ~1s window)

| payload | IronBus | NATS JetStream | Redis Streams |
| --- | --- | --- | --- |
| 256 B | 15641 | 13902 | 34524 |
| 1024 B | 5513 | 10547 | 18415 |
| 4096 B | 1359 | 6128 | 6376 |

### `memory` -- ephemeral (in-RAM, no disk)

| payload | IronBus | NATS JetStream | Redis Streams |
| --- | --- | --- | --- |
| 256 B | 84282 | 16931 | 44024 |
| 1024 B | 61229 | 17941 | 32211 |
| 4096 B | 25884 | 12419 | 15170 |

## Durable at throughput: `group-commit-fsync` (the differentiator)

Power-loss-safe AND fast: ack only after a (batched) fdatasync. IronBus reaches it on ONE connection via pipelined group commit; Redis reaches durable throughput only by running many concurrent `appendfsync=always` writers so the fsync coalesces across clients. NATS JetStream has no ack-after-fsync mode (its FileStore acks from page cache), so it has no row here. Same power-loss-safe guarantee on both sides.

| payload | IronBus (1 conn, pipelined) | Redis (50 conns, appendfsync=always) |
| --- | --- | --- |
| 256 B | 17278 | 3505 |
| 1024 B | 5622 | 2507 |
| 4096 B | 1488 | 1564 |

## Consume throughput (drain rate, durability-independent)

| payload | IronBus | NATS | Redis | Mosquitto |
| --- | --- | --- | --- | --- |
| 256 B | 250 | 22334 | 11828 | 4015 |
| 1024 B | 250 | 20416 | 10716 | 3789 |
| 4096 B | 229 | 16933 | 8455 | 3583 |

## MQTT (Mosquitto) context

MQTT is a routing protocol over broker session state, not a replayable durable log; its persistence is periodic autosave, not per-ack fsync. Reported as context (QoS 1 = at-least-once, QoS 0 = at-most-once), NOT paired head-to-head with the log systems.

| payload | QoS 1 publish | QoS 0 publish |
| --- | --- | --- |
| 256 B | 2256 | 13234 |
| 1024 B | 2249 | 12984 |
| 4096 B | 2190 | 11204 |

